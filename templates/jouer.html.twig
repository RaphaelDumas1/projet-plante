{% extends 'base.html.twig' %}
{% block header %}<h1 id="nom_plante" name="nom_plante">{{ plante.nom }}</h1>
{% endblock %}
{% block body %}   
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
        {% for info in infos %}
            <p>{{ info.texte }}</p>
        {% endfor %}
        <div id="div1"></div>
        <button id="boutton-camera">Activer l'appareil</button>
        <video id="camera" width="320" height="240" autoplay></video>
        <button id="boutton-photo" class="d-none">Click Photo</button>
        <button onclick="uploadFile()" class="d-none">upload</button>
        <canvas id="photo" name="photo" width="320" height="240"></canvas>
        <div id="ajax-path" ajax-path="{{ path('upload-plante') }}"></div>
        <script src="{{ asset('JS/app.js') }}"></script>
{% endblock %}